{
  "name": "codex-app-manager-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Official website for Codex App Manager & Codex App Mirror",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/check-security-headers.mjs",
    "preview": "vite preview",
    "fonts": "node scripts/subset-fonts.mjs",
    "images": "node scripts/optimize-images.mjs",
    "assets": "npm run fonts && npm run images"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "fontkit": "^2.0.4",
    "sharp": "^0.35.4",
    "subset-font": "^2.9.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
